# 源码来源与权威副本

## 公开候选

本仓是从桌面原工程复制后整理出的公开候选：

```text
/home/rongyi/桌面/基于树莓派的小型气象站的设计与实现/weather-station
```

历史只读封存包：

```text
/home/rongyi/桌面/基于树莓派的小型气象站的设计与实现/weather-station.zip
SHA-256: 01a3af22dbf62f9590d94709bc9c48387d54d07810358f8550d35a6d7ef3adb7
```

在公开整理开始前，排除 IDE 状态目录后，两者的 46 个有效文件逐字节一致。原工程和 ZIP 均保持只读；所有净化、文档、CI 与 Git 提交只发生在本公开候选目录。

## 公开净化

候选移除了 JetBrains `.idea/` 状态和 Avalonia 默认图标；这些内容不是实现本项目所必需。仓库不包含数据库卷、部署配置、真实用户、真实传感器记录、实物照片、视频、EDA、PCB 或制造文件。

`raspi/.env.example` 只包含不可用的描述性占位符。真实 `raspi/.env` 被 Git 忽略。

## 逐字节保留与公开整理修改

以公开整理前的 46 个有效源码文件为基线，当前候选中有 **18 个原始有效文件逐字节保持原样**；25 个原始有效文件因公开整理而修改，另有 3 项原始文件不随候选发布。整理包括将原 `docs/HARDWARE.md` 重构为仓库根目录的 `HARDWARE.md`、移除 `raspi/database/grants.sql`（固定数据库账户与弱示例口令）和 `upper/WeatherStation/Assets/avalonia-logo.ico`（默认二进制图标），并对凭据读取、JWT、默认网络绑定、错误返回、管理员初始化、Compose 暴露范围、UI 状态语义及文本行尾作了公开净化/安全修复/规范化处理。

这些修改不会回写原工程或历史 ZIP；它们的存在是为了让公开候选的边界可审计，而不是声称候选与原工程在全部文件上逐字节一致。
